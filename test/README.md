# Test Workspace

This folder is for optional test harness code.

NetBeans is configured with:

```text
test.src.dir=test
```

Use this folder for small test drivers, fake service collaborators, and repeatable checks that should not be compiled into the production application JAR.

Do not place production implementation here.
